# ベースライン実装: ディレクトリ構成

## 📁 概要

IRL+LSTMモデルとの比較のため、複数のベースラインモデルを実装します。このドキュメントでは、ベースライン実装のためのディレクトリ構成を説明します。

---

## 🗂️ ディレクトリ構成

```
gerrit-retention/
├── src/gerrit_retention/
│   ├── baselines/                    # ★ 新規：ベースラインモデル実装
│   │   ├── __init__.py               # パッケージ初期化
│   │   ├── base_baseline.py          # 基底クラス
│   │   ├── logistic_regression.py    # ロジスティック回帰ベースライン
│   │   ├── random_forest.py          # ランダムフォレストベースライン
│   │   ├── xgboost_baseline.py       # XGBoostベースライン（今後実装）
│   │   ├── vanilla_lstm.py           # Vanilla LSTMベースライン（今後実装）
│   │   └── utils.py                  # 共通ユーティリティ関数
│   │
│   └── rl_prediction/
│       └── retention_irl_system.py   # 既存：IRL+LSTMモデル
│
├── scripts/
│   ├── experiments/                  # ★ 新規：ベースライン比較実験
│   │   ├── baseline_comparison.py    # ベースライン比較のメインロジック
│   │   ├── run_baseline_comparison.py # 実行スクリプト（CLIインターフェース）
│   │   ├── visualize_comparison.py   # 結果可視化スクリプト
│   │   └── statistical_test.py       # 統計的検定スクリプト
│   │
│   └── training/irl/
│       └── train_temporal_irl_sliding_window.py  # 既存：IRL訓練
│
├── importants/
│   ├── irl_openstack_real/           # 既存：IRL実験結果
│   │
│   └── baseline_experiments/         # ★ 新規：ベースライン実験結果
│       ├── logistic_regression/      # ロジスティック回帰の結果
│       │   ├── models/
│       │   ├── predictions.csv
│       │   ├── metrics.json
│       │   └── feature_importance.png
│       │
│       ├── random_forest/            # ランダムフォレストの結果
│       │   ├── models/
│       │   ├── predictions.csv
│       │   ├── metrics.json
│       │   └── feature_importance.png
│       │
│       ├── xgboost/                  # XGBoostの結果（今後）
│       │
│       └── comparison_results/       # 全ベースライン比較結果
│           ├── comparison_metrics.csv
│           ├── comparison_summary.json
│           ├── figures/
│           │   ├── performance_comparison.png
│           │   ├── radar_chart.png
│           │   └── training_time_comparison.png
│           └── COMPARISON_REPORT.md
│
├── tests/
│   └── baselines/                    # ベースラインのユニットテスト
│       ├── test_logistic_regression.py
│       └── test_random_forest.py
│
└── docs/
    └── implementation_guides/
        ├── ベースライン実装_ディレクトリ構成.md  # このファイル
        └── IRL対抗馬_比較実験設計.md           # 実験設計書
```

---

## 📦 主要コンポーネント

### 1. `src/gerrit_retention/baselines/` - ベースラインモデル実装

各ベースラインモデルの実装を格納します。

#### `base_baseline.py` - 基底クラス

すべてのベースラインが継承する抽象基底クラス。統一されたインターフェースを提供します。

```python
from abc import ABC, abstractmethod

class BaseBaseline(ABC):
    """すべてのベースラインモデルの基底クラス"""

    @abstractmethod
    def train(self, train_data):
        """モデルを訓練"""
        pass

    @abstractmethod
    def predict(self, test_data):
        """継続確率を予測"""
        pass

    @abstractmethod
    def get_feature_importance(self):
        """特徴量重要度を取得"""
        pass

    @abstractmethod
    def save_model(self, path):
        """モデルを保存"""
        pass

    @abstractmethod
    def load_model(self, path):
        """モデルを読み込み"""
        pass
```

#### `logistic_regression.py` - ロジスティック回帰

sklearn の LogisticRegression を使用した実装。

**主な機能**:
- 時系列特徴量の集約（平均、標準偏差、トレンド）
- クラス不均衡対応（class_weight='balanced'）
- 特徴量の標準化
- 係数による特徴量重要度の出力

#### `random_forest.py` - ランダムフォレスト

sklearn の RandomForestClassifier を使用した実装。

**主な機能**:
- アンサンブル学習
- 特徴量重要度の算出
- Out-of-Bag (OOB) エラーによる検証
- ハイパーパラメータの自動チューニング（オプション）

#### `utils.py` - 共通ユーティリティ

すべてのベースラインで共有する関数。

**含まれる関数**:
- `extract_static_features()`: 時系列を集約して静的特徴量に変換
- `load_trajectory_data()`: 軌跡データの読み込み
- `evaluate_predictions()`: 予測結果の評価（AUC-PR, AUC-ROC, F1など）
- `save_results()`: 結果の保存

---

### 2. `scripts/experiments/` - ベースライン比較実験

ベースライン比較実験の実行スクリプト。

#### `baseline_comparison.py` - メインロジック

ベースライン比較の中核となるロジック。

**主な機能**:
- 複数ベースラインの一括訓練・評価
- 評価指標の計算（AUC-PR, AUC-ROC, F1, Precision, Recall）
- 訓練時間・推論時間の測定
- 結果の保存

#### `run_baseline_comparison.py` - 実行スクリプト

コマンドラインから実験を実行するためのスクリプト。

**使用例**:
```bash
# ロジスティック回帰とランダムフォレストを比較
uv run python scripts/experiments/run_baseline_comparison.py \
  --reviews data/review_requests_no_bots.csv \
  --snapshot-date 2020-01-01 \
  --history-months 12 \
  --target-months 6 \
  --baselines logistic_regression random_forest \
  --output importants/baseline_experiments/comparison_results/

# すべてのベースラインを比較
uv run python scripts/experiments/run_baseline_comparison.py \
  --reviews data/review_requests_no_bots.csv \
  --snapshot-date 2020-01-01 \
  --history-months 12 \
  --target-months 6 \
  --baselines all \
  --output importants/baseline_experiments/comparison_results/
```

#### `visualize_comparison.py` - 可視化

比較結果を可視化するスクリプト。

**生成される図**:
- 性能比較棒グラフ（AUC-PR, AUC-ROC, F1）
- レーダーチャート（多角的性能比較）
- 訓練時間比較
- 特徴量重要度比較

#### `statistical_test.py` - 統計的検定

ベースライン間の性能差が統計的に有意かを検定。

**使用する検定**:
- Wilcoxon符号順位検定
- ブートストラップ法による信頼区間推定

---

### 3. `importants/baseline_experiments/` - 実験結果

すべてのベースライン実験結果を保存します。

**出力ファイル構成**:

```
baseline_experiments/
├── logistic_regression/
│   ├── models/
│   │   └── logistic_h12m_t6m.pkl    # 訓練済みモデル
│   ├── predictions.csv               # 予測結果
│   ├── metrics.json                  # 評価指標
│   └── feature_importance.png        # 特徴量重要度の可視化
│
├── random_forest/
│   ├── models/
│   │   └── rf_h12m_t6m.pkl
│   ├── predictions.csv
│   ├── metrics.json
│   └── feature_importance.png
│
└── comparison_results/
    ├── comparison_metrics.csv        # 全ベースラインの指標一覧
    ├── comparison_summary.json       # サマリー統計
    ├── figures/
    │   ├── performance_comparison.png
    │   ├── radar_chart.png
    │   └── training_time_comparison.png
    └── COMPARISON_REPORT.md          # 比較レポート
```

---

## 🔄 ワークフロー

### ステップ1: ベースラインの訓練

```bash
# ロジスティック回帰の訓練
uv run python scripts/experiments/run_baseline_comparison.py \
  --reviews data/review_requests_no_bots.csv \
  --snapshot-date 2020-01-01 \
  --history-months 12 \
  --target-months 6 \
  --baselines logistic_regression \
  --output importants/baseline_experiments/
```

### ステップ2: 複数ベースラインの比較

```bash
# すべてのベースラインを一括実行
uv run python scripts/experiments/run_baseline_comparison.py \
  --reviews data/review_requests_no_bots.csv \
  --snapshot-date 2020-01-01 \
  --history-months 12 \
  --target-months 6 \
  --baselines logistic_regression random_forest \
  --output importants/baseline_experiments/comparison_results/
```

### ステップ3: 結果の可視化

```bash
# 可視化
uv run python scripts/experiments/visualize_comparison.py \
  --input importants/baseline_experiments/comparison_results/ \
  --output importants/baseline_experiments/comparison_results/figures/
```

### ステップ4: 統計的検定

```bash
# IRL+LSTMとの比較
uv run python scripts/experiments/statistical_test.py \
  --irl importants/irl_openstack_real/models/irl_h12m_t6m_seq.pth \
  --baselines importants/baseline_experiments/comparison_results/ \
  --output importants/baseline_experiments/comparison_results/statistical_test.json
```

---

## 📊 出力フォーマット

### `metrics.json` の例

```json
{
  "model": "logistic_regression",
  "dataset": "openstack",
  "snapshot_date": "2020-01-01",
  "history_months": 12,
  "target_months": 6,
  "metrics": {
    "auc_pr": 0.623,
    "auc_roc": 0.702,
    "f1": 0.645,
    "precision": 0.601,
    "recall": 0.695,
    "accuracy": 0.876
  },
  "training_time_seconds": 4.2,
  "inference_time_ms": 0.15,
  "model_size_mb": 0.05,
  "n_train_samples": 15243,
  "n_test_samples": 3811,
  "positive_rate": 0.085
}
```

### `comparison_metrics.csv` の例

```csv
model,auc_pr,auc_roc,f1,precision,recall,training_time,inference_time
logistic_regression,0.623,0.702,0.645,0.601,0.695,4.2,0.15
random_forest,0.698,0.756,0.687,0.652,0.725,18.5,1.2
xgboost,0.735,0.802,0.712,0.681,0.746,45.3,0.8
vanilla_lstm,0.680,0.750,0.670,0.640,0.702,1820.0,5.5
irl_lstm,0.718,0.754,0.636,0.478,0.951,2250.0,6.2
```

---

## 🎯 設計の利点

1. **モジュール化**
   - 各ベースラインが独立したモジュール
   - 新しいベースラインの追加が容易

2. **統一されたインターフェース**
   - `BaseBaseline` を継承することで一貫性を保証
   - 比較実験が自動化しやすい

3. **再現性**
   - すべてのモデルと結果が `importants/` に保存
   - 訓練済みモデルの再利用が可能

4. **拡張性**
   - 新しいベースライン（XGBoost, Vanilla LSTM, Transformer）の追加が容易
   - 実験スクリプトの拡張が簡単

5. **整理された結果**
   - ベースラインごとに結果が分離
   - 比較結果は専用ディレクトリに集約

---

## 🚀 次のステップ

1. ✅ ディレクトリ構成の設計（このドキュメント）
2. ⬜ 基底クラスの実装（`base_baseline.py`）
3. ⬜ ロジスティック回帰の実装（`logistic_regression.py`）
4. ⬜ ランダムフォレストの実装（`random_forest.py`）
5. ⬜ 共通ユーティリティの実装（`utils.py`）
6. ⬜ 比較実験スクリプトの実装（`run_baseline_comparison.py`）
7. ⬜ 可視化スクリプトの実装（`visualize_comparison.py`）
8. ⬜ 初回実験の実行

---

**作成日**: 2025年11月4日
**関連ドキュメント**:
- `docs/implementation_guides/IRL対抗馬_比較実験設計.md`
- `README.md`
- `PROJECT_OVERVIEW.md`
